---
type: Fixed
pr: 3250
---
**`/sdd-capture --seed <idea>` is one-shot again** — Trigger / Why / Scope are optional inputs with sensible defaults instead of a mandatory pre-capture questionnaire. Users capturing a stream of ideas no longer get blocked between writes.
