---
type: Fixed
pr: 3260
---
**`/sdd-settings` Intel question now points to the correct command** — was telling users to use the retired `/sdd-intel` (folded into `/sdd-map-codebase --query` by #2790). Same correction applied to `references/planning-config.md`, `docs/USER-GUIDE.md`, `docs/FEATURES.md`, `docs/INVENTORY.md`, and `agents/sdd-intel-updater.md`. No backend change.
